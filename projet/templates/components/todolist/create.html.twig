{% extends 'base.html.twig' %}

{% block title %}Créer une nouvelle liste{% endblock %}

{% block body %}
    <h1>Créer une nouvelle liste de tâches</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Créer</button>
    {{ form_end(form) }}

    <a href="{{ path('list_todolists') }}">Retour aux listes</a>
{% endblock %}
